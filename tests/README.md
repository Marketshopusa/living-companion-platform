# Tests

## Gate 1 identity

```text
python -m pip install -r requirements-dev.txt
python -m pytest tests/identity
```

Or `scripts/check-identity-contracts.ps1` / `scripts/check-identity-contracts.sh`.

Gate 0 layout/secrets: `scripts/check-baseline.ps1` / `scripts/check-baseline.sh`.
