# Local development placeholders only. Copy to .env (gitignored).
# Do not put real secrets in this file or in source control.
# Gate 0: no paid providers. Leave unset.

LCP_ENV=local
# LCP_LLM_PROVIDER=local_stub
# LCP_STT_PROVIDER=local_stub
# LCP_TTS_PROVIDER=local_stub
